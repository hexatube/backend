name: app check

on: push

jobs:
  elixir-check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: erlef/setup-beam@v1.17.5
        with:
          otp-version: '25'
          elixir-version: '1.16.3'
      - run: mix deps.get
      - run: mix test